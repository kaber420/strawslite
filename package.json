{
  "name": "straws-monorepo",
  "private": true,
  "workspaces": [
    "strawslite",
    "straws"
  ],
  "scripts": {
    "build:engine": "npm run build:engine -w strawslite",
    "build:lite:firefox": "npm run build:firefox -w strawslite",
    "build:lite:chrome": "npm run build:chrome -w strawslite",
    "build:lite:all": "npm run build:lite:firefox && npm run build:lite:chrome",
    "build:manual:firefox": "npm run build:firefox -w straws",
    "build:manual:chrome": "npm run build:chrome -w straws",
    "build:manual:all": "npm run build:manual:firefox && npm run build:manual:chrome",
    "build:all": "npm run build:engine && npm run build:lite:all && npm run build:manual:all",
    "test:lite": "npm test -w strawslite",
    "test:manual": "npm test -w straws"
  }
}
